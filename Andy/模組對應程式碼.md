FinMem 各模組對應的程式碼位置
以下連結可以直接點開跳到 VS Code 對應的那一行。路徑都是相對於 D:\Claude\金融AI\FinMem 這個工作目錄。

1. Summarize(摘要)
data-pipeline/03-summary.py:25 — summary = model.summarize,呼叫下面 model_wrapper 的摘要函式,對每一列新聞/財報執行摘要並存回 CSV
data-pipeline/03-model_wrapper.py:11 — Model_Wrapper.summarize(),對外統一的摘要入口,帶重試機制
data-pipeline/03-model_wrapper.py:24 — Chatgpt._summarize(),實際組出「把這段文字壓成 {N} tokens」的提示詞、呼叫 LLM API
2. Profiling Module(角色設定)
config/tsla_gpt_config.toml:12 — character_string,你要編輯的人設文字本體,寫在設定檔裡
puppy/agent.py:92 — LLMAgent.__init__ 的 character_string 參數
puppy/agent.py:103 — self.character_string = character_string,存成 Agent 的固定屬性,這行執行完之後就不會再變
puppy/agent.py:160 — from_config() 讀取設定檔的 character_string 傳進建構子
puppy/agent.py:189、212、233、259 — 四次 query_text=self.character_string,分別對應淺/中/深/反思四層的查詢,這裡才是它唯一被讀取使用的地方
3. Sorting(分類)
puppy/agent.py:565 — LLMAgent.step(),整個每日流程的主體
puppy/agent.py:580 — # 1. handling filings,呼叫 _handling_filings,10-Q/10-K 依型態分別存進中層/深層
puppy/agent.py:167 — _handling_filings() 本體
puppy/agent.py:584 — # 2. handling news,呼叫 _handling_news,新聞存進淺層
puppy/agent.py:179 — _handling_news() 本體
puppy/agent.py:586 — # 3. update the price to portfolio,股價交給 Portfolio(往 Observe 那條路走)
4. Observe(觀察)
puppy/portfolio.py:88 — get_moment(moment_window=3),把最近 3 天收盤價相減後加總,判斷漲/跌/持平
5. Layered Long-term Memory(分層長期記憶)
puppy/memorydb.py:32 — MemoryDB 類別,單一層的資料存取與分數管理
puppy/memorydb.py:84 — add_memory(),新記憶誕生時,對每一筆各自抽籤決定重要性分數、給滿分新近度
puppy/memorydb.py:248 — _decay(),每天的衰退計算
puppy/memorydb.py:270 — _clean_up(),新近度或重要性任一過低就刪除
puppy/memorydb.py:301 — prepare_jump(),檢查哪些記憶達到升降層門檻
puppy/memorydb.py:356 — accept_jump(),實際搬遷,升層才重置新近度
puppy/memorydb.py:461 — BrainDB 類別,把淺/中/深/反思 4 個 MemoryDB 包成一個整體
puppy/memorydb.py:681 — BrainDB.step(),驅動四層各自完成當天的衰退、清倉、升降層
puppy/memory_functions/importance_score.py:31、38、45 — 淺/中/深三張機率表,決定新記憶誕生時抽到幾分
puppy/memory_functions/decay.py:5 — ExponentialDecay,衰退公式與衰退係數
puppy/memory_functions/access_counter.py:1 — LinearImportanceScoreChange,importance_score += access_counter * 5
puppy/memory_functions/compound_score.py:1 — LinearCompoundScore,綜合分數與 merge_score 的計算公式
6. Retrieve(檢索)
puppy/memorydb.py:138 — MemoryDB.query(),拿查詢向量對這一層做相似度搜尋,結合綜合分數排名,取 Top-K
7. Reflect(反思推理)
puppy/reflection.py:354 — trading_reflection(),Reflect 的主函式,組提示詞、呼叫 LLM、驗證回傳格式
puppy/prompts.py:31 — train_prompt,訓練模式的提示詞模板
puppy/prompts.py:41 — test_prompt,測試模式的提示詞模板
puppy/chat.py:73 — ChatOpenAICompatible.guardrail_endpoint(),真正發送 HTTP 請求給 LLM API 的地方
8. Investment decisions and reflecting outcomes(投資決策及結果反映)
puppy/agent.py:460 — _construct_train_actions(),Train 模式直接用隔日實際漲跌決定方向,不看 LLM
puppy/agent.py:530 — __process_test_action(),Test 模式把 LLM 的買/賣/持有轉成方向數字
puppy/agent.py:464 — _portfolio_step(),把方向寫進持倉紀錄
puppy/agent.py:545 — _update_access_counter(),回顧視窗內的實際損益,對這次引用過的記憶加分或扣分
puppy/portfolio.py:53 — get_feedback_response(),算出賺/賠/打平的回饋值,並標記是哪一天的引用該被評分
puppy/agent.py:596610 — step() 裡的第 58 步,依序呼叫上面這幾個函式,收尾這一天