# FinMem 建置注意事項

這個專案已經從官方預設的 OpenAI + Vertex AI 版本，改成一套完全跑 Gemini
免費版的流程（詳見 `Andy/FinMem_免費版執行計畫.md`）。

## 從零開始的完整步驟（build 指令已實際重新驗證過）

**README.md 裡寫的 `docker build -t test-finmem finmem/.devcontainer/.` 是
官方模板留下的指令，路徑對不上這個倉庫的實際結構（沒有 `finmem/` 這層
資料夾），照著打會建置失敗。實際能用、且已經重新驗證過的指令如下：**

```powershell
# 1. 建置映像檔（build context 是整個 repo 根目錄，不是 finmem/.devcontainer/.）
docker build -f .devcontainer/Dockerfile -t finmem:py310 .

# 2. 啟動容器並掛載原始碼（映像檔內 WORKDIR 是 /workspace/ofcourseistillloveyou，
#    不是 README 說的 /finmem）
docker run -it --rm -v ${PWD}:/workspace/ofcourseistillloveyou finmem:py310 bash

# 3. 容器內確認環境正常（已驗證：puppy 底下每個模組都能正常 import，
#    run.py --help / run.py sim --help 顯示的參數跟 run_gemini.sh 用的旗標完全對上）
python run.py sim --help

# 4. 把 .env.example 複製成 .env 並填入下面「需要準備的金鑰」章節列的三把金鑰，
#    然後執行
bash run_gemini.sh
```

**已知這個映像檔建置時會多裝一套 `google-cloud-cli`（apt 來源）**，這是
Dockerfile 裡沿用官方模板、給舊版 Vertex AI 流程用的（`gcloud auth`），
現在改用 Gemini API Key 直接打 AI Studio 端點，**完全用不到 gcloud**，
裝了也不影響，只是會讓建置多花一點時間，不用特地移除。

下次重新 clone 之後，**不要照官方 README 剩下的步驟走**，會踩到已經解決過的雷，
直接照這份文件走。

## 已經放棄的官方 API（不要再申請）

- **OpenAI API Key**：已改用 Gemini 取代 chat 與 embedding，不需要。
- **HF_TOKEN**：不使用 TGI／gated 的 HuggingFace 模型，不需要。
- **Alpaca API**：申請流程要求提供真實稅務識別碼（KYC），已改用 Tavily
  取代新聞來源，避免交出身分資訊。**不要為了這個專案申請 Alpaca。**

## 需要準備的金鑰（填進 `.env`，原本的 `.env` 只有佔位文字已被刪除）

| 變數 | 用途 | 備註 |
|---|---|---|
| `GEMINI_API_KEY` | Chat（決策）＋ Embedding（記憶向量化） | AI Studio 免費金鑰，不是 Vertex AI |
| `SEC_KEY` | 抓真實 10-K/10-Q 財報 | **終身額度只有 100 次**，全專案共用，省著用 |
| `TAVILY_API_KEY` | 抓真實新聞 | 免費 1,000 額度/月，email 註冊即可，不用信用卡 |

## 版本鎖定：補上原本沒鎖版本的 14 個套件

`.devcontainer/requirements.txt` 原本已經對大多數套件鎖了確切版本，但
`yfinance` 本身沒鎖（浮動最新版），連帶它的 transitive 依賴
（`pandas`、`beautifulsoup4`、`curl_cffi`、`cffi`、`pycparser`、
`multitasking`、`peewee`、`platformdirs`、`protobuf`、`pytz`、
`tzdata`、`websockets`、`soupsieve`）也都沒被鎖到。現在已經全部改成實測
建置成功那次 `pip freeze` 抓到的確切版本（`yfinance==1.6.0`、
`pandas==2.3.3` 等），之後重新建置不會再因為這些套件出新版而重新踩雷。

## Python 版本要求

`pyproject.toml` 限定 `>=3.10,<3.11`。如果本機 Python 版本更新過（例如變成
3.14），要另外建立 3.10 的虛擬環境，不能直接用系統預設版本，否則依賴安裝會出錯。

## 官方原始 pipeline 腳本裡發現的 bug（已改用自寫腳本繞過，不要用官方版）

`data-pipeline/04-data_pipeline.py` 和 `05-get_sentiment_by_ticker.py`
已知有問題：
- 回傳的 tuple 格式跟 `puppy/environment.py` 期待的 `OneDateRecord` 格式對不上。
- 程式碼裡有打字錯誤：一邊叫 `filing_q`、一邊叫 `filling_q`，兩個名字對不起來。

**解法是不要用這兩支官方腳本**，改用專案裡新增的三支替代腳本：
`fetch_price.py`（抓股價）、`fetch_news_tavily.py`（抓新聞）、
`fetch_filing_sec.py`（抓財報，改寫自官方的
`data-pipeline/01_SEC_API_10k10q_download.py`，官方版股票清單是空的佔位字串
且日期區間沒設，直接跑會抓不到東西），最後用 `merge_dataset.py` 把三份資料
合併成 `puppy/environment.py` 要的格式。

## Gemini 相關的程式碼修改

- `puppy/chat.py`：原本寫死打 Vertex AI 端點（`elite-destiny-371016` 這個
  已經沒有免費額度的專案），改成用 API Key 直接打 AI Studio 端點，不用
  `gcloud auth`；另外加上 429（速率限制）自動重試/節流邏輯——**如果之後遇到
  跑到一半卡住不動，先檢查是不是撞到 Gemini 免費層的速率限制，不是程式壞了**。
- `puppy/embedding.py`：新增了 Gemini embedding 的 class，取代原本寫死呼叫
  `OpenAIEmbeddings` 的版本。
- `config/tsla_gemini_config.toml`：模型與 embedding 設定已經指向免費版
  （`gemini-3.5-flash-lite`、`gemini-embedding-001`），不要改回舊的
  `gemini-pro` / Vertex AI 端點設定。

## 額度與時間預估（供參考，不是硬性限制）

以半年（126 個交易日）的資料量估算：Embedding 呼叫約 630 次、Chat 呼叫約
150 次，都在 Gemini 免費層每日 1,000 次上限內；不用跨天執行，一次坐著跑
大約 45 分鐘到 1 小時內能跑完訓練＋測試流程。

## 已知不在 git 版控內的東西

- `data/` 底下的原始資料、訓練輸出、checkpoint 這次已經連同程式碼一起推上
  GitHub 了，重新 clone 下來就有現成的示範資料可以直接用，不用整套重抓。
